export enum AuthProviders {
    GITHUB = "github",
    GITLAB = "gitlab",
    DISCORD = "discord",
    GOOGLE = "google",
    CREDENTIAL = "credential"
}

export enum UserSessionStates {
    ACTIVE = "active",
    UNVERIFIED = "unverified"
}