import { SITE_NAME_SHORT } from "@root/config";

const App = () => {
    return (
        <div className="content">
            <h1>{SITE_NAME_SHORT}</h1>
        </div>
    );
};

export default App;
